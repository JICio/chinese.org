name = "chinese-tts"
main = "worker.js"
compatibility_date = "2026-08-01"

[vars]
# Azure 語音資源所在區域（建議 eastasia，離大陸用戶最近）
AZURE_REGION = "eastasia"

# 密鑰不放這裡！部署後用命令設置（只進 Cloudflare 加密存儲）：
#   npx wrangler secret put AZURE_TTS_KEY
